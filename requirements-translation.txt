# Optional local translation dependencies.
# Run Setup-GPU-Acceleration.bat afterward when an NVIDIA CUDA build of
# PyTorch is desired for the main translation environment.
transformers>=4.51,<5
sentencepiece>=0.2,<1
sacremoses>=0.1,<1
torch>=2.4
huggingface_hub>=0.34
